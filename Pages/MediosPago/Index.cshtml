@page
@model ReservaCabanasSite.Pages.MediosPago.IndexModel
@{
    ViewData["Title"] = "Medios de Pago";
}

<style>
.btn-custom {
    padding: 8px 22px;
    border-radius: 6px;
    border: none;
    font-weight: 600;
    cursor: pointer;
    text-decoration: none;
    transition: background 0.2s;
}
.btn-primary-custom {
    background: #0097b2;
    color: #fff;
}
.btn-primary-custom:hover {
    background: #00798e;
    color: #fff;
}
.btn-secondary-custom {
    background: #5c4a45;
    color: #fff;
}
.btn-secondary-custom:hover {
    background: #4a3c38;
    color: #fff;
}
.btn-danger-custom {
    background: #00353e;
    color: #fff;
}
.btn-danger-custom:hover {
    background: #c0392b;
    color: #fff;
}
.btn-success-custom {
    background: #27ae60;
    color: #fff;
}
.btn-success-custom:hover {
    background: #229954;
    color: #fff;
}

.table-container {
    background: white;
    border-radius: 12px;
    box-shadow: 0 2px 16px rgba(44,62,80,0.08);
    padding: 24px;
    margin: 20px 0;
}

.table-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
}

.table-title {
    font-size: 1.5rem;
    font-weight: bold;
    color: #5c4a45;
}

.status-badge {
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.8rem;
    font-weight: 600;
    text-transform: uppercase;
}

.status-active {
    background: #b0dfe7;
    color: #155724;
}

.status-inactive {
    background: #f8d7da;
    color: #721c24;
}

.table th {
    background: #f8f9fa;
    border-bottom: 2px solid #dee2e6;
    color: #5c4a45;
    font-weight: 600;
    padding: 12px 8px;
}

.table td {
    padding: 12px 8px;
    vertical-align: middle;
    border-bottom: 1px solid #dee2e6;
}

.table tbody tr:hover {
    background: #f8f9fa;
}

.table tbody tr.inactivo {
    background-color: #ffe6e6;
}

.table tbody tr.inactivo:hover {
    background-color: #ffd6d6;
}

.action-buttons {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
}

.btn-sm {
    padding: 4px 12px;
    font-size: 0.8rem;
}
</style>

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Medios de Pago</h1>
    <a asp-page="Create" class="btn-custom btn-primary-custom">Nuevo Medio de Pago</a>
</div>

<div class="table-container">
    <div class="table-header">
        <div class="table-title">Lista de Medios de Pago (@Model.MediosPago.Count)</div>
    </div>

    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                    <th>Fecha Creación</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var medio in Model.MediosPago)
                {
                    <tr class="@(medio.Activo ? "" : "inactivo")">
                        <td>@medio.Id</td>
                        <td><strong>@medio.Nombre</strong></td>
                        <td>@(medio.Descripcion ?? "-")</td>
                        <td>
                            @if (medio.Activo)
                            {
                                <span class="status-badge status-active">Activo</span>
                            }
                            else
                            {
                                <span class="status-badge status-inactive">Inactivo</span>
                            }
                        </td>
                        <td>@medio.FechaCreacion.ToString("dd/MM/yyyy")</td>
                        <td>
                            <div class="action-buttons">
                                <a asp-page="Edit" asp-route-id="@medio.Id"
                                   class="btn-custom btn-primary-custom btn-sm @(medio.Activo ? "" : "disabled")">Editar</a>
                                @if (medio.Activo)
                                {
                                    <a asp-page="Inactivate" asp-route-id="@medio.Id"
                                       class="btn-custom btn-danger-custom btn-sm">Inactivar</a>
                                }
                                else
                                {
                                    <form method="post" asp-page-handler="Activar" asp-route-id="@medio.Id" style="display:inline">
                                        <button type="submit" class="btn-custom btn-success-custom btn-sm">Activar</button>
                                    </form>
                                }
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (!Model.MediosPago.Any())
    {
        <div class="text-center py-4">
            <p class="text-muted">No hay medios de pago registrados.</p>
        </div>
    }
</div>
